# car-puzzle
Car-movement puzzle solver using Z3 (SMT). Supports manual .txt puzzles and random generation, finds minimal solutions via bounded planning (incremental horizon), and can export the full SMT-LIB2 encoding for debugging/reproducibility.
